import theme from "../src/theme"

const styles = {
  paper: { display: 'flex', justifyContent: 'center', padding: theme.spacing(0.4, 0.4, 0.4, 1.4), marginTop: '20px' },
  containerMargin: { marginTop: '50px' },
}

export default styles